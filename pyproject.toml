[project]
name = "threejs-pyside6"
version = "0.1.0"
description = "Three.js scene embedded in a PySide6 desktop window"
requires-python = ">=3.11"
dependencies = [
    "pyside6>=6.8.0",
    "numpy>=2.0",
]
